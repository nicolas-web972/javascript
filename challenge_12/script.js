'use strict';

const temperatures1 = [17, 21, 23];

for (let i = 0; i < temperatures1.length; i++) {
  console.log(`${temperatures1[i]} degrés dans ${i + 1} jour(s).`);
};

const temperatures2 = [12.5, 5, -5, 0, 4];

for (let i = 0; i < temperatures2.length; i++) {
  console.log(`${temperatures2[i]} degrés dans ${i + 1} jour(s).`);
};
